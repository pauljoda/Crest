# Guide screenshots

Store help-center screenshots in a folder named for the article slug, for example:

`static/img/guides/install-chrome-web-store/review.png`

Use the reusable `GuideScreenshot` component, provide useful alt text, and keep captions focused on the action the screenshot clarifies. Stable article-specific paths let screenshots change without changing public guide URLs.
