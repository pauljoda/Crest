import Foundation

/// App-wide motion preference, independent of Space branding and sidebar motion.
enum SpacePageMotionPreference {
    static let key = "crest.appearance.animate-space-pages"
    static let defaultValue = true
}
