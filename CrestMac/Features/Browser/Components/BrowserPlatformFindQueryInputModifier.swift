import SwiftUI

/// macOS leaves the find field with the system's own text behaviour.
struct BrowserPlatformFindQueryInputModifier: ViewModifier {
    func body(content: Content) -> some View {
        content
    }
}
