import CoreGraphics

/// macOS interaction geometry consumed by the shared semantic layout tokens.
enum CrestPlatformLayout {
    static let minimumHitTarget: CGFloat = 28
    static let sidebarRowHeight: CGFloat = 40
}
