{
  "manifest_version": 3,
  "name": "Crest Popup Reload Probe",
  "description": "Reproduces the post-two-factor popup sweep that reloads every chrome.extension.getViews() entry other than the popup's own window.",
  "version": "1.0.0",
  "permissions": [
    "offscreen",
    "storage"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Popup Reload Probe"
  }
}
