# Implementation progress on main

The audit and test-retention cleanup were merged into local `main` at `80846dd9`. Implementation proceeds directly in `/Users/pauldavis/Dev/Crest`, as requested. The original audit remains a report of baseline `457ac1d2`; the comparison for this implementation starts at merged main `80846dd9`, which includes the subsequent Space setup and sidebar-menu work.

Current status: Infrastructure path consolidation is committed at `34401446`. Native Space paging moves retained view frames and keeps chrome stationary; inspected command frames show separate page labels and retain the arriving page after settling. N reduces the measured spring-request-to-motion interval to 34–40 ms from M's 167–188 ms. The complete performance milestone remains open: physical swipe acceptance, post-completion work and cold/idle extension behavior are still outstanding.

## First implementation slice

- Replace the horizontal sidebar ScrollView with bounded, retained sidebar pages keyed by Space/profile. The first Mac implementation used threshold-triggered SwiftUI offset animation; physical validation rejected it. Candidate F moved Mac viewport presentation into AppKit, using continuous native swipe progress over prepared page layers and committing browsing selection after the visual swipe settled. Physical input confirmed that it follows the fingers, and the user reported improvement, but rejected its slow movement and visible end pop. Candidate G now replaces normalized swipe progress with precise scroll deltas in points and velocity-aware native settling. Its focused tests and optimized build passed, and physical feedback reported better motion but rejected the settle flash and delayed icon-strip movement. Candidate H removed the measured background flash, but physical feedback still rejected a visible snap and choppiness at selection. Candidate I moves address and navigation controls outside the moving tab pages, as requested. K replaces unsupported backing-layer transforms with actual native view movement; L and M narrow sidebar state propagation. Their measured results and remaining delays are recorded below. Remembered unloaded tabs must retain the existing Start Page entry policy.
- Preserve one adjacent destination per horizontal gesture, vertical scrolling, momentum, cancellation, and direction. The user clarified that motion must follow the fingers immediately; waiting for a threshold and then serializing fixed animations does not satisfy that requirement. Native settling must allow fresh input to interrupt it. The existing touch presentation stays in its mobile adapter; shared selection, access checks, and accessibility remain shared.
- Build current/saved section insertion maps once before row iteration. This removes repeated full-run mapping for each row while preserving split, folder, and drag targets.
- Avoid page-residency sweeps when the full Space values match the last reconciliation. Keep the extension event/focus pass for Start Page, locked, and empty destinations. Full Space changes and initial evaluation still use full reconciliation. Equality is not claimed to be constant time when array storage differs.
- Resolve extension metadata from the requested authorized live tab rather than reconstructing every Space on each property read. Keep detached state, transient tabs, auxiliary windows, reader/loading state and permission behavior. Lookup still searches the owning Space's tabs; this is not a constant-time index.
- Keep stable address/editing bindings and forward them without observing their values in five Mac sidebar containers. The address controls retain live reads and writes; cached bindings use weak model captures. This follows an initial profile in which broad address-state propagation remained expensive.
- Keep extension sidebar visibility bookkeeping but avoid options notifications for tab-only changes and empty-registration visibility changes. A later installation receives the remembered availability and pending opens still run.
- Move native title dismissal, palette controls and Getting Started icon fallback into platform owners; shared views use those adapters. Restore the architecture guard after the newer main changes without adding exceptions.

## Retained coverage and cleanup

Remove the obsolete horizontal-scroller observer/suppressors and delayed recenter request. Move the existing touch transition cancellation/coalescing cases into the mobile suite with their owning model. Keep one gesture sequence suite and one native-event integration test for the wheel adapter, including vertical pass-through and disable/re-enable boundaries. Add two native viewport contracts covering actual fractional native-view movement before semantic selection, one accepted completion, reversal, refused selection receipts, interrupted settling, and stale/external/drag-lock cancellation. Extend the existing extension suites with one fresh-live-metadata/operation-count case and one deferred-install visibility case. No permanent test is added for path movement, a layout constant, or an implementation-only counter.

The Mac run before the later drag-identity, geometry, and native-pager revisions passed **248 tests, zero failures or skips**, including the retained native editing cases. The initial Mac run exposed a readiness race: document JavaScript could respond before WebKit delivered the native title update. The existing test now waits for both conditions; its behavior assertions remain.

The corrected mobile batch passed **191 of 192 tests**; the remaining archive case passed in a subsequent isolated run. The archive deadline was three seconds while the failing batch logged a 3.359-second WebContent process launch; the same unchanged test passed in the initial run and isolated recheck. This is not a clean full-suite result. Three earlier reorder failures came from stale fixtures (68-point pinned tiles versus the actual 47-point layout, promotion first occurring at the destination, and the old duplicate gap-reservation assertion). Correct the fixture geometry and source promotion, and assert the actual anchored split gap and cancellation; preserve all destination and cross-Space checks. At that earlier run, reorder state/layout production sources were identical to merged main and no mobile test was added or deleted. The later stable drag-source adapters and native-pager integration require fresh validation. The two touch transition cases now move from the Mac suite to the existing mobile navigation suite, matching their actual owner.

F's native integration passed **90 focused Mac tests, zero failures or skips**: four gesture/viewport cases, the retained drag/assignment/window cases, native address editing, and 18 page-residency/selection cases. Both platforms compiled, including the shared selection receipt signature. The 35 affected mobile reorder, drag-adapter, and moved-transition cases passed in two focused runs; both task-owned simulators were deleted. Temporary offscreen probes also confirmed common `.global` window coordinates and namespace matching across nested hosting views. F's layer transforms left hit-test geometry stationary during motion, so the native viewport blocked row interaction until it restored coherent resting geometry. K subsequently replaced that unsupported geometry ownership with actual native view frames. The probes do not establish presented-frame timing or palette animation quality.

## Measurement and current performance status

Use optimized, isolated `Crest Performance Soak` builds, six Spaces with 24 tabs and eight expanded saved folders each, and one explicitly loaded resident tab per Space. Disable the fixture's periodic title mutation in the temporary served copy identically before and after. Native input automation uses the already authorized Computer Use plugin. Stop automation while the user physically tests the app.

The initial implementation **did not meet the performance goal**. In the verified 12-switch, accessibility-assisted pair, baseline main-thread sampled weight was 25,753 ms over 40.876 seconds and candidate B was 29,231 ms over 40.975 seconds: 13.5% more sampled work. Every input and destination verification fit within its trace. Empty-extension options notifications disappeared from samples, but that local improvement did not produce a faster overall interaction.

The user then reported roughly a second before a swipe activated. A passive 30.866-second profile, with no automated input or accessibility queries, recorded ten potential hangs of 1.05–1.35 seconds. Main-thread sampled weight was 19,425 ms, including 16,395 ms under hosting layout and only 171 ms under accessibility. Real scroll-handler stacks were present. This confirms substantial layout work during physical use; it does not timestamp the user's initial finger movement.

Candidate C deferred unrelated neighbor preparation and preserved row identities when drag-source availability changed. It reduced main-thread sampled work in the same keyboard/accessibility-assisted workload to 12,850 ms over 40.928 seconds, but physical user feedback still rejected the interaction. Candidate D localized moving-list geometry observation and corrected active-folder ownership; it also failed user acceptance. These are not gesture-performance success claims.

A diagnostic-only E build captured eight real trackpad gestures. Event timestamp to accepted threshold was 20–63 ms, with the selection callback under 2 ms. Selection returned 72–133 ms before the pager observed it; the subsequent native draw milestone followed presentation by another 311–705 ms. Acceptance to that CPU-side draw milestone was 408–899 ms. Six gestures queued behind prior animation completions. All generations completed with their current selection; no cancellation or disabled-animation flags explained the missing motion. The two passive CPU recordings missed these eight gestures, so their stacks are not attributed to these event intervals.

This evidence changes the implementation direction: transient viewport progress must not depend on a committed browsing-state change or repeated SwiftUI offset layout. Apple's [native swipe tracker](https://developer.apple.com/documentation/appkit/nsevent/trackswipeevent(options:dampenamountthresholdmin:max:usinghandler:)) supplied continuous callbacks for candidate F. Installed Arc/Dia class metadata also establishes dedicated native sidebar paging controllers and cached views, but reveals neither their source nor timing curves.

F's physical burst contained 23 tracking generations and 1,559 progress callbacks. The last two gestures, after both build logs stopped, reached their first nonzero layer update 1.316 and 1.085 ms after the native event timestamp. These are CPU-side event-to-layer timings, not presented frames. Twenty-two initial event/callback pairs scaled the input by 0.001; multiplying that normalized amount by the approximately 289-point sidebar moved it only about 0.289 points per precise input unit. The tracker consumed subsequent raw events, so this establishes initial scaling rather than a full-stream speed cap. Those last two gestures also took 736 and 814 ms from finger-end phase to native completion. Two cancellations jumped approximately 17.5 and 30 points back to zero. Successful completions themselves ended within 0.028 points of their final position; source inspection instead found accessory insertion and an abrupt global branding change at semantic selection. Three event-queue outliers over 100 ms followed previous commits and overlapped builds; post-commit UI work and build contention remain unresolved contributors without a matching CPU trace.

G uses [precise scrolling distance](https://developer.apple.com/documentation/appkit/nsevent/scrollingdeltay) directly, then a native spring with recent gesture velocity, preserving the presentation position when new input interrupts a settle. An offscreen Core Animation probe established that this runtime normalizes spring velocity to the signed remaining distance. Incoming resident sidebars now read their own assignment-checked address metadata, and docked branding crossfades on selection. The existing tests now await actual animation completion instead of injecting a synthetic completion. All 90 focused Mac tests passed, followed by a four-test viewport rerun after the final receipt and external-selection corrections. The optimized G build passed and its executable and matching dSYM were preserved with verified UUIDs and hashes. Physical feedback found better movement but rejected the settle flash and delayed icons. Nonadjacent programmatic selections present directly; adjacent commands and all physical swipes use native motion, avoiding a compressed distant jump that cannot be rebased onto immediate neighbors.

G's physical release logs also exposed an edge-velocity mismatch: already-resisted page motion was handed back to the spring at full raw input velocity. H converts release velocity through the same resistance derivative. A 12-second, single-window command-path recording then captured the reported backdrop dip: teal and sand patches darkened together for recorded samples spanning roughly 58–75 ms. The fixture's six Spaces share identical branding, yet G replaced and opacity-faded the entire atmosphere on each Space ID. H preserves that layer and animates only actual branding changes. The baseline recording is an adjacent keyboard-command comparison, not a physical gesture-performance result.

The icon strip previously read only committed selection, toggled a static per-icon background, then ran a separate deferred recenter animation. H provides a stable, non-Observable presentation reference read only by a native picker leaf. Raw point updates and a spring-only display link supply the pager layer's actual position; the leaf interpolates measured icon frames and native clip-view scrolling. Browser selection and sidebar rows do not observe per-frame progress. Existing buttons, semantic accessibility selection, manual overflow controls, and customization remain. H passed 143 focused Mac tests and both platform builds. Its optimized executable and matching dSYM are preserved. Subsequent physical feedback still rejects the selection handoff; the frame and CPU findings below identify remaining work.

The first matching H command recording contains no repeat of G's backdrop dip under the same patch comparison. Its icon lane nevertheless stays at the same fractional position through most early frames, then shifts about 13 video pixels at PTS 0.262 seconds while the outline remains centered. This is not evidence of synchronized icon movement. That capture explicitly queried accessibility immediately after sending the command. A repeat without the query subsequently showed 14 successive fractional icon positions over roughly 267 ms, confirming that the query materially distorted the first clip; it does not establish a separate picker defect. Identical fixture icons also prevent distinguishing a whole-icon ordinal swap from an unchanged grid. Native model updates, sampled CPU timing, and encoded frames remain separate evidence.

H's three adjacent commands without explicit accessibility queries during recording still produced 666–727 ms native completion callbacks for configured 400 ms springs. Sampled main-thread hosting layout was 323–353 ms per command window (including the 500 ms after completion), with tab/folder bodies and value equality in the stacks. Synchronous page activation accounted for 1–2 ms sampled and web-view attachment about 1 ms per command. Instruments also identified 406 ms and 319 ms microhangs. These measurements implicate broad SwiftUI updates rather than expensive synchronous page loading in this warm fixture. The earlier physical burst contained 126 gestures with complete raw-to-point coverage, but its 256 ms maximum first-event delay was mostly time waiting in the event queue. That burst was outside the later CPU recordings; the passive 120-second CPU/video pair captured no gestures and is retained only as idle evidence.

I mounts navigation, URL editing, and pinned extension controls once above the pager. Their state transitions respect Reduce Motion, while only the Space header, pinned tabs, folders, and tab list move. Cached pages no longer contain duplicate address/focus state. The measured fixed-control height extends the native gesture region without moving the controls or claiming web-content gestures. The stationary site-control popover closes on Space/profile changes. The optimized I Mac build and 90 retained Mac interaction, privacy, editing, residency, and gesture cases passed. Its app, matching dSYM, and 2,085 input-source hashes/bytes are preserved before further edits. The matched three-command, no-accessibility-query recording verifies fixed chrome and moving tab/folder content. Focused main-thread samples decreased from H's 1,573 ms to 1,339 ms (14.9%), but 298–307 ms microhangs and 626–658 ms completion callbacks remain. Extra picker diagnostics are a comparison confound; this is not a clean single-variable attribution.

J narrows each cached page's stored context so it does not carry the entire available-Space collection; it does not skip environment, callback, or binding updates. Its optimized build passed, but its prepared web page remained blank, so a matched CPU comparison was withheld. Later physical input contained 30 gestures and 25 commits; first-event queuing reached 183 ms while selection callbacks stayed below 3.4 ms. The user's overlapping-text screenshot rejects this candidate. A passive recording began after the overlap had cleared and did not reproduce it.

The overlap investigation identified unsupported native geometry ownership: every page had the same NSView frame, while direct backing-layer transforms separated the pages. Apple's [rules for AppKit-backed layers](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/CoreAnimation_guide/CreatingBasicAnimations/CreatingBasicAnimations.html) reserve these transforms for AppKit. An offscreen probe with both direct NSHostingViews and wrapper views reproduced coincident pages after actual frame origin/size changes. Unchanged-frame root refreshes and layout did not reproduce it; the precise frame-change trigger in the user's screenshot remains uncorrelated. K therefore moves the retained native views through NSView frame APIs, uses a custom frameOrigin animator spring, samples presentation frames, and clips each page. Interruption removes only the pager wrapper's animations and restores its captured native position; the probe verified subsequent one-point movement after flush and run-loop processing. The existing native contracts now assert actual frame separation after root refresh and resize, stopped spring presentation after interruption, and adjacent admission during rapid successive commands. A newly admitted page receives its correct offscreen frame before insertion. This establishes geometry correctness, not yet full-sidebar performance or physical acceptance.

A separate web-host ownership regression was also reproduced: after rejecting one stale attachment, clearing the old host's weak page identity allowed a second stale update to steal the page from its new host. Retaining that identity rejects repeated stale attachment while still allowing legitimate reclaim after detachment. The strengthened existing contract failed before the change and passed afterward with 34 focused web-host/page-pool cases. This does not yet establish the cause of the blank fixture page. Temporary host identity and geometry logs remain in measured builds for correlation and will be removed from the production handoff.

K's optimized build and all 119 focused Mac cases passed; its executable, matching dSYM and 2,085 input sources are preserved. The direct 6→3 fixture return was initially blank, so the ownership change is not a complete explanation or fix for that behavior. One adjacent preparation restored both actual HTML subtrees at load count one. The three-command recording then retained both loaded pages and showed fixed chrome, separated moving pages and no coincident labels in the inspected frames. Focused main-thread weight was 1,383 ms versus I's 1,339 ms (3.3% more in one run), with 634/629/659 ms completion callbacks and two associated 260/333 ms microhangs. The whole trace also contains a later 271 ms microhang outside those command windows. Its keys began later in the recording than I's, so whole-trace weight is not the preferred comparison. Web-host logs show three detachments and attachments across two identities, with mounted nonzero geometry and no stale rejection. Native-frame ownership fixes the demonstrated geometry flaw; these measurements do not establish a latency improvement or physical acceptance.

Splitting K at the first nonzero native presentation sample identifies 241/236/254 ms before movement starts, containing 224/215/240 ms of sampled hosting layout. The subsequent sampled movement spans 393/393/405 ms; another 125/127/113 ms of main-thread work follows completion. The configured spring is delayed before it starts rather than simply spending its duration behind blocked rendering. Middle-command encoded frames show approximately 358–383 ms of visible travel; the video time origin is not precisely synchronized with OSLog, so an exact input-to-presented-frame delay is not claimed.

A separate SwiftUI Instruments diagnostic capture directly records `@Observable BrowserStore.session` as an upstream update source for tab-row surfaces, rows, close controls, folders, headers and their ForEach children in the command intervals. Its filtered View Body table is empty; graph records must not be reported as body invocation counts or durations. L targets that dependency with one immutable shared per-Space presentation value: assignment, current role, unlock state, membership, branding and split metadata. Display availability ignores global selection so inactive controls keep their normal appearance; drag registration and deferred editor presentation retain the captured current role. Every action's existing live authorization remains unchanged. Three expansion getters use supplied folder data, while toggling derives the next state from the exact live folder to preserve consecutive actions. Mobile/previews retain the previous live fallback. L's optimized Mac build, mobile simulator build, architecture guards and all 120 focused Mac cases passed, including one retained stale-presentation authorization contract.

L's comparison found only a 2.0% reduction in focused main-thread samples (1,355 ms versus K's 1,383 ms), with 202–229 ms before sampled movement and two 303/281 ms microhangs. Initial hosting layout decreased to 212/182/212 ms, while post-completion work rose to 126/147/141 ms. The inspected frames remain clean, and both fixture pages retain actual HTML at load count one. This is insufficient improvement. Its presentation value still carried selection through complete row configurations, and Mac saved-tabs expansion still read the whole observed session while constructing the list.

M separates the selected role from immutable display data. Only existing drag-source, full-folder-container, current-tab-zone and editor-cleanup modifiers read that role. Current-page registration remains opt-in at the same original callers; pinned-grid behavior and owned-lift exceptions are preserved. Zone eligibility participates in its optional measured value so returning at an unchanged frame restores registration. Mac ordinary content no longer carries an unused selected-role argument, and page callbacks capture their actual owners and Space instead of the entire selected-page view. Utility-only selection stays in its utility leaf. The Mac saved-tabs getter reads the supplied Space value, while an optional shared header action reads the live exact selected/unlocked Space before toggling; Mobile/previews keep the previous binding action. All 120 focused Mac cases, optimized Mac build and mobile build pass, with 2,087 input sources and matching app/dSYM preserved. No additional permanent test method was added for this refinement. M's three focused command windows contain 1,151 ms of main-thread sample weight versus L's 1,355 ms (15.1% less) and K's 1,383 ms (16.8% less). Before the first nonzero presentation sample, hosting layout is 162/150/169 ms and elapsed time is 182/167/188 ms. Completion callbacks remain 579/560/581 ms for configured 400 ms springs, and one 318 ms Instruments microhang remains. The clean frame sequence keeps controls fixed and retains the incoming page after settling. This is a single-run comparison with a material preparation confound: M needed one additional adjacent readiness check and was older at capture (approximately 190 seconds versus L's 103); the final quiet intervals both match approximately 45 seconds. It is not a clean causal estimate or physical-swipe acceptance.

After profiling, Computer Use separately verified that the current Space's saved-tabs header collapses and restores all eight folders. A documented two-click call also returned to Expanded with eight folders; its short clip does not resolve an intermediate collapsed render, so it is not proof of two separately displayed toggles. No source change, build, accessibility query or automated UI inspection ran during the matched CPU/video recording. The M app remains open for physical slow-swipe, release and reverse-swipe feedback.

N removes the duplicate root refresh at the start of an adjacent external-selection slide. Ready native views move immediately, and completion supplies the latest content, environment and callbacks. Changing a Space's privacy policy invalidates motion immediately; resize cancellation and window reattachment refresh current roots before restoring input. The existing native lifecycle case covers latest role/content after completion and these cancellation paths, including a policy change during physical motion. The optimized build and 16 affected gesture/assignment cases passed before measurement.

Its matched three-command recording contains 850 ms of focused main-thread samples versus M's 1,151 ms (26.2% less). First nonzero native presentation follows the spring request by 34/40/34 ms rather than 182/167/188 ms; pre-motion hosting layout falls to 26/24/22 ms. Callback intervals are 439/435/435 ms for the 400 ms spring. Some work moves later: post-completion main-thread weight is 154/148/155 ms versus M's 111/124/112 ms. Instruments reports no potential hangs in the 31.005-second N trace; that does not mean every frame met its deadline. The quiet periods are 45.0 versus 44.7 seconds; N's launch age is 203.5 seconds versus M's 189.9, with the same extra adjacent preparation pair and loaded HTML at load count one. This is a single-run command comparison, not a physical latency percentile.

The inspected N video keeps navigation/address controls stationary, moves clipped live page content and shows one Space's labels after settling without the earlier backdrop flash. Middle-command visible travel spans approximately 367 ms in the encoded frames. Exact video/OSLog synchronization and physical finger movement are not established. The initially blank direct 6→3 fixture return still needed adjacent preparation; no claim is made that all blank or cold-load cases are fixed.

For the production handoff, remove 145 temporary diagnostic lines, their logger file and generated project references, plus five temporary test-counter lines. Retain the behavioral lifecycle/authorization assertions and existing supported performance harness. Final production-source verification passed: optimized Mac build, mobile simulator build, all 120 focused Mac cases, 26 existing script guard/catalog cases, architecture and vertical-structure checks, and strict formatting for 45 changed Swift files. The source stayed unchanged across both builds. No temporary diagnostic counter or logger remains in production code. The exact instrumented N executable, matching dSYM, source snapshot and raw capture remain preserved separately for reproducibility.

Time Profiler samples and potential-hang intervals are not presented-frame timestamps, physical trackpad calibration, ordinary-user switch latency, or p50/p95/p99 animation results. Inclusive stack categories overlap. Address mutation appears to trigger an already pending synchronous graph flush; its inclusive cost does not establish that all flushed work belongs to address subscribers. Keep thermal state, process age, command timestamps and recording boundaries with the evidence.

## Infrastructure organization

The complete Shared Infrastructure migration is applied across 266 Swift files: 259 path simplifications (102 extension-related and 157 in other domains), with seven already clean files unchanged. Related domains now have simpler paths without redundant Browser prefixes; preflight found no cross-platform basename collisions. Every moved source preserves its exact pre-move bytes and file mode, including public symbols and persisted/runtime identifiers. The eight current extension-service documentation rows now point to the new paths, and 51 historical audit source links are pinned to the original audit baseline. All 259 files passed strict formatting before movement. XcodeGen has regenerated the project, and every target retains exactly the same source membership under the path mapping: 2,056 Mac app sources, 1,737 mobile app sources, and unchanged test sources, with no duplicate entries. The mobile build passed. The first Mac build was intentionally interrupted when physical input began; the subsequent focused Mac test build and optimized G build both passed with all migrated paths. The Infrastructure-only naming guard also accepts a simplified extension filename only when its complete extension-target set is the single matching Browser-prefixed owner; its existing eight contract tests and eleven other repository guard tests pass.

## Rapid swipe follow-up (0.5.84)

Physical feedback accepted the clearer moving pages but exposed repeated interruptions before settling. A native viewport reproduction with six short flicks showed the old origin advancing through indices 0–5 while visible position remained close to the first page. The last three interruptions had no visible page; final selection jumped to index 6. Restarting from the nearest page at the actual presentation position keeps the bounded host set aligned with what is visible. One sample anchors the entire strip, and release projection includes inherited displacement.

Native animation tests also exposed AppKit choosing an older presentation value as the spring's default start, even when tracking had already changed the native frame. Different pages could consequently restart from incompatible positions. An isolated AppKit probe measured a requested start of −224 points being replaced with 0; explicitly supplying the spring's `fromValue` preserved −224 and the expected subsequent motion. Each page now gets that explicit native start through the existing AppKit frame animator, with no forced layout flush or view replacement. This follows Apple's documented [animation customization](https://developer.apple.com/documentation/appkit/nsanimatablepropertycontainer/animation(forkey:)) and [interpolation start value](https://developer.apple.com/documentation/quartzcore/cabasicanimation/fromvalue).

The permanent suite adds one focused repeated-interruption contract covering early successive flicks, later reversal, inherited slow continuation, visible coverage and final selection in both layout directions. Existing cancellation, privacy, receipt and drag-lock cases remain. All 18 affected Mac cases pass. Temporary diagnostic prints and standalone probes stay out of production sources. This validates the reproduced lifecycle failure; physical feel remains a separate check.

## Direct settling and fresh flicks (0.5.85)

The follow-up reproduced a short second flick being spent completing the first transition. In a 320-point viewport, the incoming page still had about 75 points to travel. Subtracting that offset from a fresh 32-point flick at 1,000 points/second rejected the new destination, although the same flick qualified from rest. Release now counts the greater of fresh gesture travel and already-visible progress, so an unfinished incoming offset cannot penalize a fresh flick. Interruption still captures the real visible position, and slow continuation retains its earlier progress.

A separate native viewport probe released a page 20 points before its endpoint at 4,000 points/second. The previous spring overshot the endpoint by 37 points before returning. Settling now uses a bounded ease-out timing curve with a 100–220 ms duration; release speed can shorten the transition without introducing spring velocity. Native frame samples in the same probe move directly from −300 to −320 points, never passing the endpoint. This uses Apple's [basic animation timing curves](https://developer.apple.com/documentation/quartzcore/camediatimingfunction) rather than a [spring that can overshoot its target](https://developer.apple.com/documentation/quartzcore/caspringanimation).

The existing interruption test now includes the short successive flick and checks actual presentation frames for monotonic settling in both layout directions. The old spring-velocity assertion was replaced with the requested direct-settling behavior. All 18 focused macOS tests and the optimized Mac build passed. A separate phased `NSEvent` probe, using the production gesture handler and velocity calculation with controlled event samples, also rejects the second destination before the change and accepts it afterward; intervening momentum remains consumed. These probes exercise native views and their rendered presentation positions; they do not establish physical trackpad feel in the full application.

## Remaining work

The [implementation plan](implementation-plan.md) remains the ordered direction for full structural revision separation, narrower row observations, extension readiness/recovery and preparation, the remaining name/owner consolidation, and shared page lifecycle behavior. This slice does not claim to solve the Dark Reader cold/idle popup or page-load delays. Physical trackpad/wheel calibration, frame/hitch instrumentation, contrasting-brand motion, larger sessions and phone interaction remain acceptance work for the complete Space performance milestone.
