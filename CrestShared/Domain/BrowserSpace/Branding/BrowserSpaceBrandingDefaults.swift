import Foundation

/// The values a Space's branding starts from, named once so editors can offer
/// "back to default" without restating them.
enum BrowserSpaceBrandingDefaults {
    static let folderColorIntensity: Double = 0
    static let textColorMode = BrowserSpaceTextColorMode.automatic
}
