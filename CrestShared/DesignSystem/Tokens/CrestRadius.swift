import CoreGraphics

/// Controlled corner radii. Crest uses tight chrome rather than pill-shaped content.
enum CrestRadius {
    static let compact: CGFloat = 8
    static let control: CGFloat = 12
    static let card: CGFloat = 16
    static let panel: CGFloat = 24
}
