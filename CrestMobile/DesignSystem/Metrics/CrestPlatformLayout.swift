import CoreGraphics

/// iOS and iPadOS interaction geometry consumed by shared layout tokens.
enum CrestPlatformLayout {
    static let minimumHitTarget: CGFloat = 44
    static let sidebarRowHeight: CGFloat = 44
}
