import SwiftUI
import UIKit

/// Mobile grounds the floating sidebar card in layered system chrome.
enum BrowserPlatformFloatingSidebarCardStyle {
    static var backgroundColor: Color {
        Color(uiColor: .secondarySystemBackground)
    }
}
