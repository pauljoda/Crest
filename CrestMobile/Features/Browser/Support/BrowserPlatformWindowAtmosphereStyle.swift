import SwiftUI
import UIKit

/// Mobile grounds the window atmosphere in the base system background.
enum BrowserPlatformWindowAtmosphereStyle {
    static var backgroundColor: Color {
        Color(uiColor: .systemBackground)
    }
}
