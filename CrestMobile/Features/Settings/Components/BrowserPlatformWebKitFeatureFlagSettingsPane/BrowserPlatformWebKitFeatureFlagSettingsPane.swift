import SwiftUI

/// WebKit's private runtime feature registry is intentionally a macOS-only
/// direct-distribution surface.
struct BrowserPlatformWebKitFeatureFlagSettingsPane: View {
    var body: some View {
        EmptyView()
    }
}
