namespace CrestCore.Contracts;

/// <summary>What a new window shows first: the Start Page, or the tab that was active
/// when Crest last quit.</summary>
public enum StartupBehavior { ShowStartPage, LastActiveTab }
